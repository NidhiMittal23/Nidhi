'use strict';

describe('myApp.license module', function() {

  beforeEach(module('myApp.license'));

  describe('license controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var licenseCtrl = $controller('licenseCtrl');
      expect(licenseCtrl).toBeDefined();
    }));

  });
});